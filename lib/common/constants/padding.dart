import 'package:flutter/widgets.dart';

class CustomPadding {
  static const pdefault =
      EdgeInsets.only(bottom: 30, left: 30, right: 30, top: 10);

  static const p1 = EdgeInsets.all(10);
  static const p2 = EdgeInsets.all(20);
  static const p3 = EdgeInsets.all(30);

  static const ps1 = EdgeInsets.symmetric(horizontal: 10);
  static const ps2 = EdgeInsets.symmetric(horizontal: 20);
  static const ps3 = EdgeInsets.symmetric(horizontal: 30);

  static const px1 = EdgeInsets.symmetric(horizontal: 10);
  static const px2 = EdgeInsets.symmetric(horizontal: 20);
  static const px3 = EdgeInsets.symmetric(horizontal: 30);

  static const py1 = EdgeInsets.symmetric(vertical: 10);
  static const py2 = EdgeInsets.symmetric(vertical: 20);
  static const py3 = EdgeInsets.symmetric(vertical: 30);
}
